---
"@cloudflare/nimbus-docs": minor
"@cloudflare/create-nimbus-docs": minor
---

Render canonical content collections through the Cloudflare Workers adapter with request-safe partial headings, 404 responses, and build-derived syntax-highlighting assets. Cloudflare server scaffolds now enable request rendering by default, the interactive scaffolder only exposes the supported Cloudflare adapter, and existing customized projects receive an agent-ready configuration runbook.
