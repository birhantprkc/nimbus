---
"@cloudflare/nimbus-docs": patch
"@cloudflare/create-nimbus-docs": patch
---

Allow user-owned Astro pages, endpoints, and scaffolded publication routes to use native rendering semantics while retaining entrypoint-aware checks for active Nimbus contracts and composing with unrelated integration routes. Scaffolded dynamic publication endpoints now resolve their prepared artifacts when rendered on request.
